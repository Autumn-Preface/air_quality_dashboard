# air_quality_dashboard
Client-side AQI dashboard. Upload CSV, explore 7+ charts (trends, heatmap, pie, boxplot, radar, bar, scatter). Filter by date/city. Theme toggle, PNG export. No backend, runs in browser. Built with ECharts &amp; Papa Parse. GitHub Pages ready.
